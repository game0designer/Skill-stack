# Navigate to project root (where client, server, database folders are)
# cd C:\your-project-path

# Create shared directory
New-Item -ItemType Directory -Force -Path "shared" | Out-Null
Set-Location "shared"

# Create subdirectories
New-Item -ItemType Directory -Force -Path "types" | Out-Null
New-Item -ItemType Directory -Force -Path "constants" | Out-Null
New-Item -ItemType Directory -Force -Path "utils" | Out-Null
New-Item -ItemType Directory -Force -Path "validators" | Out-Null
New-Item -ItemType Directory -Force -Path "schemas" | Out-Null
New-Item -ItemType Directory -Force -Path "enums" | Out-Null
New-Item -ItemType Directory -Force -Path "interfaces" | Out-Null

Write-Host "✅ Shared directories created" -ForegroundColor Green

# Create TypeScript type definition files
New-Item -ItemType File -Force -Path "types\user.types.js" | Out-Null
New-Item -ItemType File -Force -Path "types\post.types.js" | Out-Null
New-Item -ItemType File -Force -Path "types\comment.types.js" | Out-Null
New-Item -ItemType File -Force -Path "types\api.types.js" | Out-Null
New-Item -ItemType File -Force -Path "types\index.js" | Out-Null

# Create constant files
New-Item -ItemType File -Force -Path "constants\roles.js" | Out-Null
New-Item -ItemType File -Force -Path "constants\status.js" | Out-Null
New-Item -ItemType File -Force -Path "constants\tags.js" | Out-Null
New-Item -ItemType File -Force -Path "constants\errors.js" | Out-Null
New-Item -ItemType File -Force -Path "constants\config.js" | Out-Null
New-Item -ItemType File -Force -Path "constants\index.js" | Out-Null

# Create utility files
New-Item -ItemType File -Force -Path "utils\formatters.js" | Out-Null
New-Item -ItemType File -Force -Path "utils\helpers.js" | Out-Null
New-Item -ItemType File -Force -Path "utils\dateUtils.js" | Out-Null
New-Item -ItemType File -Force -Path "utils\stringUtils.js" | Out-Null
New-Item -ItemType File -Force -Path "utils\index.js" | Out-Null

# Create validator files
New-Item -ItemType File -Force -Path "validators\auth.validator.js" | Out-Null
New-Item -ItemType File -Force -Path "validators\post.validator.js" | Out-Null
New-Item -ItemType File -Force -Path "validators\user.validator.js" | Out-Null
New-Item -ItemType File -Force -Path "validators\comment.validator.js" | Out-Null
New-Item -ItemType File -Force -Path "validators\index.js" | Out-Null

# Create schema files
New-Item -ItemType File -Force -Path "schemas\user.schema.js" | Out-Null
New-Item -ItemType File -Force -Path "schemas\post.schema.js" | Out-Null
New-Item -ItemType File -Force -Path "schemas\comment.schema.js" | Out-Null
New-Item -ItemType File -Force -Path "schemas\notification.schema.js" | Out-Null
New-Item -ItemType File -Force -Path "schemas\index.js" | Out-Null

# Create enum files
New-Item -ItemType File -Force -Path "enums\role.enum.js" | Out-Null
New-Item -ItemType File -Force -Path "enums\postStatus.enum.js" | Out-Null
New-Item -ItemType File -Force -Path "enums\notificationType.enum.js" | Out-Null
New-Item -ItemType File -Force -Path "enums\actionType.enum.js" | Out-Null
New-Item -ItemType File -Force -Path "enums\index.js" | Out-Null

# Create interface files
New-Item -ItemType File -Force -Path "interfaces\IUser.js" | Out-Null
New-Item -ItemType File -Force -Path "interfaces\IPost.js" | Out-Null
New-Item -ItemType File -Force -Path "interfaces\IComment.js" | Out-Null
New-Item -ItemType File -Force -Path "interfaces\IApiResponse.js" | Out-Null
New-Item -ItemType File -Force -Path "interfaces\index.js" | Out-Null

# Create root shared files
New-Item -ItemType File -Force -Path "index.js" | Out-Null
New-Item -ItemType File -Force -Path "package.json" | Out-Null

Write-Host "✅ All shared files created" -ForegroundColor Green
Write-Host ""
Write-Host "📁 Shared folder structure:" -ForegroundColor Cyan
Write-Host "shared/" -ForegroundColor Yellow
Write-Host "├── types/           (5 files)" -ForegroundColor Gray
Write-Host "├── constants/       (6 files)" -ForegroundColor Gray
Write-Host "├── utils/           (5 files)" -ForegroundColor Gray
Write-Host "├── validators/      (5 files)" -ForegroundColor Gray
Write-Host "├── schemas/         (5 files)" -ForegroundColor Gray
Write-Host "├── enums/           (5 files)" -ForegroundColor Gray
Write-Host "├── interfaces/      (5 files)" -ForegroundColor Gray
Write-Host "├── index.js" -ForegroundColor Gray
Write-Host "└── package.json" -ForegroundColor Gray
